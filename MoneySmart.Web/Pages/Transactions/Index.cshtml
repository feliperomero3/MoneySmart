@page
@model IndexModel

@{
    ViewData["Title"] = "Transactions";
}

<h1>Transactions</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<div class="table-responsive">
    <table class="table table-sm table-striped table-bordered">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Transactions[0].DateTime)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Transactions[0].Description)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Transactions[0].TransactionType)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Transactions[0].Amount)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Transactions[0].AccountName)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Transactions)
            {
                <tr>
                    <td>
                        <a asp-page="./Details" asp-route-id="@item.Id">@Html.DisplayFor(modelItem => item.DateTime)</a>
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TransactionType)
                    </td>
                    <td class="text-right">
                        @Html.DisplayFor(modelItem => item.Amount)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.AccountName)
                    </td>
                    <td>
                        <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
